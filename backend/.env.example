# Default Gemini Model to use
DEFAULT_MODEL=gemini-3-pro-preview

# Your Google Cloud Project ID (Optional: defaults to gcloud config / ADC)
GCP_PROJECT_ID=

# Your Google Cloud Location for Vertex AI (e.g., us-central1)
GCP_LOCATION=global

# Google Cloud Storage bucket for video uploads (Optional)
GCS_BUCKET=
